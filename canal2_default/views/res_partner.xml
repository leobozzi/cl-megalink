<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Res Partner Form view -->
    <record model="ir.ui.view" id="custom_res_partner_form_view">
        <field name="name">customt_res_partner_form_view</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <field name="parent_id" position="after">
                <field name="ref" />
            </field>
        </field>
    </record>

    <!-- Res Partner Tree view -->
    <record model="ir.ui.view" id="custom_res_partner_tree_view">
        <field name="name">customt_res_partner_tree_view</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree" />
        <field name="arch" type="xml">
            <field name="display_name" position="after">
                <field name="ref" optional="show"/>
                <field name="customer_payment_mode_id" optional="hide"/>
            </field>
        </field>
    </record>
</odoo>