<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Account Move Res Partner Form view -->
    <record model="ir.ui.view" id="custom_account_move_form_view">
        <field name="name">customt_account_move_form_view</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="partner_ref" />
            </field>
        </field>
    </record>

    <!-- Account Move Res Partner Tree view -->
    <!--
    <record model="ir.ui.view" id="custom_account_move_tree_view">
        <field name="name">customt_account_move_tree_view</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_out_invoice_tree" />
        <field name="arch" type="xml">
            <field name="invoice_partner_display_name" position="after">
                <field name="partner_ref" optional="show"/>
            </field>
        </field>
    </record>
    -->
</odoo>